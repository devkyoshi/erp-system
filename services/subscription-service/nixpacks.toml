[phases.setup]
nixPkgs = ["go_1_23"]

[phases.install]
cmds = [
    "go mod download"
]

[phases.build]
cmds = [
    "CGO_ENABLED=0 GOOS=linux go build -o subscription-service ./cmd/main.go"
]

[start]
cmd = "./subscription-service"
